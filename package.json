{
  "name": "convex-conversation-explorer",
  "version": "0.0.1",
  "repository": "git@github.com:koreanwglasses/ConvEx-revision-1.git",
  "author": "koreanwglasses <koreanwglasses@gmail.com>",
  "scripts": {
    "dev:client": "cd client && yarn start",
    "dev:server": "cd server && yarn dev",
    "dev": "npm-run-all --parallel dev:client dev:server",
    "format:client": "cd client && yarn format",
    "format:server": "cd server && yarn format",
    "format": "npm-run-all format:client format:server"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
